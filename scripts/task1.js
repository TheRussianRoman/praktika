let q = 2;
operation = (x,y) => Math.pow(y,q)+Math.abs(x)+Math.cos(q*y)

// Создайте функцию operation, которая будет возвращать решение
// функции f(x) = y^q+|x|+ cos(qy), где q – номер в списке.